<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appdigital.projetointegradorbandasecasasdeshow">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.MainActivity">

        </activity>
        <activity android:name=".cadastro.Cadastro_Banda" />
        <activity android:name=".cadastro.Cadastro_De_Novo_Usuario" />
        <activity android:name=".login.LoginAll" />
        <activity android:name=".login.ProfileActivity" />
        <activity
            android:name=".usuario.Usuario_Banda"
            android:label="@string/title_activity_usuario__banda"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".usuario.Usuario_Casa_Show"
            android:label="@string/title_activity_usuario__casa__show"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>